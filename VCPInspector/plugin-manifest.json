{
    "name": "VCPInspector",
    "displayName": "VCP 系统自检器",
    "version": "0.1.0",
    "description": "VCP 配置一致性检查、元思考链健康度校验、外部文件 Diff 审计。校验 chains 引用的 cluster 是否存在于向量库、kSequence 长度是否匹配、缓存是否过期、是否缺少 themeKeywords 等。",
    "author": "纳西妲",
    "pluginType": "hybridservice",
    "entryPoint": {
        "script": "VCPInspector.js"
    },
    "communication": {
        "protocol": "direct"
    },
    "webSocketPush": {
        "enabled": false
    },
    "configSchema": {
        "DebugMode": {
            "type": "boolean",
            "description": "是否输出调试日志",
            "default": false
        }
    },
    "capabilities": {
        "invocationCommands": [
            {
                "command": "CheckMetaThinking",
                "description": "校验元思考链配置健康度：chains 引用的 cluster 是否存在于向量库、kSequence 长度是否匹配、缓存是否过期、是否缺少 themeKeywords 等。"
            }
        ]
    }
}